[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "py14days"
version = "0.1.0"
description = "A 14-day Python learning project"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
